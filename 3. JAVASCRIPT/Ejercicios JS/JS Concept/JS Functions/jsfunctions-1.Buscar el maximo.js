/* Iteración #1: Buscar el máximo */
/* Completa la función que tomando dos números como argumento devuelva el más alto. */
function sum(numOne, numberTwo) {
  let numOne = 2;
  let numberTwo = 5;
  let numeroAlto = "";

  if (numOne > numberTwo) {
    return numOne;
  } else {
    return numberTwo;
  }
}

sum(numeroAlto);
